# Uptisement — IG inbound DM demo

**Live:** https://adil-044.github.io/uptisement-ig-dm-demo/

Conversational qualify → Calendly preview. Same brain as vault ManyChat pack:

`vaultsousxchef/500 - Exports/clients/uptisement/ig-inbound-dm/`

## Local

Open `index.html` or:

```bash
python3 -m http.server 8765 --directory /root/uptisement-ig-dm-demo
```

## Meta

Uptisement has no Page/IG yet — follow `CONNECT-CHECKLIST.md` then paste `MANYCHAT-COPY.md`.
